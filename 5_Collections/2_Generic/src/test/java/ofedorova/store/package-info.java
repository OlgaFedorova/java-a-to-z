/**
 * Сделать интерфейс Store<T extends Base> где Base Это абстрактный класс для моделей c методами
 * String getId();
 * void setId(String id).
 * Сделать два класса User, и Role которые наследуют данный класс.
 * Сделать два хранилища UserStore и RoleStore. Внутри для хранения данных использовать SimpleArray.
 */
package ofedorova.store;